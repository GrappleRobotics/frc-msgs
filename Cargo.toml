[package]
name = "frc-can"
version = "0.1.0"
edition = "2021"

[dependencies]
bxcan = "0.6.0"
serde = { version = "1.0.159", optional = true, features = ["derive"] }
strum_macros = "0.24.3"

[features]
std = []
serde = ["dep:serde"]
default = ["std", "serde"]
